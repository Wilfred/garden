struct Foo {
    x: Int,
    y: String,
}

fun main(args: List): Unit {
    // Should this use the string_repr now?
    // dbg(Foo { x: 1 + 2, y: "abc" });

    print(string_repr(Foo { x: 1 + 2, y: "abc" }));
}

// args: run
// expected stdout: Foo {y: "abc", x: 3}
