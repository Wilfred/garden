public fun foo() {
  "a" + "b"
}

// args: check
// expected exit status: 1
// expected stdout:
// Error: You can only use `+` for `Int` values. For `String` values, use `^` instead.
// 
// -| src/test_files/check/string_concat_with_plus.gdn:2:7
// 1| public fun foo() {
// 2|   "a" + "b"
// 3| }     ^

// expected stderr: 
// 1 issue can be fixed automatically (use `--fix`).
