import "__shell.gdn" as shell

{
    let (stdout, stderr) = shell::run("sh", ["-c", "echo out; echo err 1>&2"]).or_throw()
    dbg(stdout)
    dbg(stderr)
}

// args: run
// expected stdout:
// "out\n"
// "err\n"
