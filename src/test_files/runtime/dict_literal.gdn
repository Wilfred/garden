// Use a function to verify the inferred type too.
fun get_dict(): Dict<String> {
  Dict["a" => "hello", "z" => "", "b" ^ "c" => "world"]
}

dbg(get_dict())

// args: run
// expected stdout: Dict["a" => "hello", "bc" => "world", "z" => ""]

