import "__shell.gdn" as shell

fun main() {
  let count = shell::get_env("COUNT").or_throw().as_int().or_throw()

  println("start")

  let i = 0
  while i < count {
    i += 1
  }

  println("finish")
}

main()
