// TODO: Loading these one-by-one into REPL stops us getting positions
fun two(b) {
    if (b) {}
}

fun one(x) {
    two(x);
}

fun call_g() { two("a string"); }

fun main(args) {
    one("not bool");
}
