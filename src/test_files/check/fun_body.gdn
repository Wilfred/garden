public fun returns_fun(): Fun<(Int), String> {
  // We should only complain about the lambda body, not the whole
  // lambda.
  fun(x) { x + 1 }
}

// args: check
// expected exit status: 1
// expected stdout:
// Error: Expected `String`, but got `Int`.
// -| src/test_files/check/fun_body.gdn:4:12
// 2|   // We should only complain about the lambda body, not the whole
// 3|   // lambda.
// 4|   fun(x) { x + 1 }
// 5| }          ^^^^^

