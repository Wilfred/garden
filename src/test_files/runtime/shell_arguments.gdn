{
  // This is only arguments after the .gdn file name, so it's an empty
  // list.
  dbg(shell_arguments())
  return
}

// args: run
// expected stderr: [src/test_files/runtime/shell_arguments.gdn:4:3] shell_arguments() //-> []
