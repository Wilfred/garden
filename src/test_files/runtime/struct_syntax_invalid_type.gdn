enum Foo { X }

{
    Foo{ x: 1 }
}

// args: run
// expected stderr:
// Error: `Foo` is not a struct, so it cannot be initialized with struct syntax.
// --> src/test_files/runtime/struct_syntax_invalid_type.gdn:4	 __toplevel__
//     Foo{ x: 1 }
//     ^^^

