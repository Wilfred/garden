import "__shell.gdn" as shell

test shell_is_unsafe {
  shell::run("ls", ["/"])
  // ^
}

// args: sandboxed-test
// expected stdout:
// {"description":"sandboxed","tests":{"shell_is_unsafe":{"description":"sandboxed","failure_start_offset":null,"failure_end_offset":null}}}
