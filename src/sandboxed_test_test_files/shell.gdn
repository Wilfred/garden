test shell_is_unsafe {
    shell("ls", ["/"])
    // ^
}

// args: sandboxed-test
// expected exit status: 1
// expected stderr:
// src/sandboxed_test_test_files/shell.gdn:2: Tried to execute unsafe code in sandboxed mode.

