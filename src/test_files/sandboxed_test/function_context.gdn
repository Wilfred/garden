fun foo() {
    // Cursor isn't inside a test, so just run all the tests in this
    // file.
    print("")
    // ^

}

test one {
    
}

test two {
}

// args: sandboxed-test
// expected stdout:
// {"description":"2 passed","tests":{"two":{"description":"passed","failure_start_offset":null,"failure_end_offset":null},"one":{"description":"passed","failure_start_offset":null,"failure_end_offset":null}}}

