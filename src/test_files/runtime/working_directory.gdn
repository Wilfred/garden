import "__fs.gdn" as fs

{
  fs::set_working_directory(Path{ p: "/tmp" })
  dbg(fs::working_directory())
}

// args: run
// expected stderr:
// [src/test_files/runtime/working_directory.gdn:5:3] fs::working_directory() //-> Path{ p: "/tmp" }

