// Incomplete
fun bar() {

// This should be fully parsed.
fun baz() {}

// The nested `if` block swallows the only closing brace below, so
// `qux` itself is left unclosed rather than the `if`.
fun qux(): Int {
  if True {
    1
}

fun quux(): Int {
  2
}

// args: check
// expected exit status: 1
// expected stdout:
// Error: Expected `}` to close this.
// 
// -| src/test_files/check/incomplete_fun.gdn:2:11
// 1| // Incomplete
// 2| fun bar() {
// 3|           ^
// 4| // This should be fully parsed.
// -| src/test_files/check/incomplete_fun.gdn:2:11
// 1| // Incomplete
// 2| fun bar() {
// 3|           ^ Note: The `{` here is never closed.
// 4| // This should be fully parsed.
// 
// Error: Expected `}` to close this.
// 
// --| src/test_files/check/incomplete_fun.gdn:12:1
// 10|   if True {
// 11|     1
// 12| }
//   | ^
// 13| 
// 14| fun quux(): Int {
// --| src/test_files/check/incomplete_fun.gdn:9:16
//  7| // The nested `if` block swallows the only closing brace below, so
//  8| // `qux` itself is left unclosed rather than the `if`.
//  9| fun qux(): Int {
// 10|   if True {    ^ Note: The `{` here is never closed.
// 11|     1

